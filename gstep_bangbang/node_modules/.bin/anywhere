#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.npminstall/anywhere/1.2.0/anywhere/bin/anywhere" "$@"
  ret=$?
else 
  node  "$basedir/../.npminstall/anywhere/1.2.0/anywhere/bin/anywhere" "$@"
  ret=$?
fi
exit $ret
